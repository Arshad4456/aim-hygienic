# aim-hygienic

## Environment

Set `MONGODB_URI` to the database you want to use (the last path segment controls the database name). For production, point it at your real database name instead of the default `test` used by many MongoDB tools.

Example:

```
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/aim_hygienics
```
