"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
export default function PortalSidebar({ menu = [], user }) { const pathname = usePathname(); return <aside className="hidden w-72 shrink-0 border-r border-slate-800 bg-slate-950 text-white lg:block"><div className="p-5"><div className="rounded-2xl bg-gradient-to-br from-emerald-500 via-cyan-500 to-blue-600 p-4 shadow-lg"><p className="text-xs uppercase tracking-[0.25em] text-white/75">Rawyan ERP</p><h2 className="mt-1 text-xl font-black">Portal</h2><p className="mt-1 text-xs text-white/75">{user?.fullName || user?.username || "User"}</p></div></div><nav className="space-y-1 px-3 pb-6">{menu.map((item) => { const active = pathname === item.path || pathname.startsWith(`${item.path}/`); return <Link key={item.key} href={item.path} className={`block rounded-xl px-4 py-3 text-sm font-semibold transition ${active ? "bg-white text-slate-950 shadow" : "text-slate-300 hover:bg-white/10 hover:text-white"}`}>{item.name}</Link>; })}</nav></aside>; }
