import { RAWYAN_BRAND } from "@/src/app/config/brand";
import { rawyanGradients } from "./gradients";
import { designTokens } from "./designTokens";

export const rawyanTheme = { brand: RAWYAN_BRAND, gradients: rawyanGradients, tokens: designTokens };
export default rawyanTheme;
