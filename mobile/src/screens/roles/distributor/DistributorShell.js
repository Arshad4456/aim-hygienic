import React from 'react';
import RoleShellBase from '../../common/RoleShellBase';

export default function DistributorShell({ navigation }) {
  return <RoleShellBase label="Distributor" navigation={navigation} />;
}
