import React from 'react';
import RoleShellBase from '../../common/RoleShellBase';

export default function DeliveryBoyShell({ navigation }) {
  return <RoleShellBase label="Delivery Boy" navigation={navigation} />;
}
