import React from 'react';
import RuntimeModuleScreen from '../common/RuntimeModuleScreen';

export default function GenericRuntimeModuleScreen({ moduleItem }) {
  return <RuntimeModuleScreen moduleItem={moduleItem} />;
}